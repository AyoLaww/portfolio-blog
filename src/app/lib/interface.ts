export interface blogLinks{
    title: string,
    currentSlug: string,
    publishedAt: string,
}

export interface blogPage{
    currentSlug: string, 
    title: string,
    content: any,
    publishedAt: string,
}